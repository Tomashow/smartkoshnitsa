We finished a working feature. Do a checkpoint:

1. Run all tests: python3 -m pytest tests/ -v
2. If any fail: fix them first
3. Show git status
4. Suggest a commit message for what was just built
5. Do NOT commit — just suggest the message
